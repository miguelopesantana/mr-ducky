# Postgres
POSTGRES_PASSWORD=changeme

# API auth
JWT_SECRET=change-me-in-production
ADMIN_USER_EMAIL=admin@example.com
# Generate: python -c "from passlib.context import CryptContext; print(CryptContext(['bcrypt']).hash('yourpassword'))"
ADMIN_USER_PASSWORD_HASH=

# Integrations (optional for Phase 0)
ANTHROPIC_API_KEY=
GOCARDLESS_SECRET_ID=
GOCARDLESS_SECRET_KEY=
