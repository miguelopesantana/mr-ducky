# Postgres
POSTGRES_PASSWORD=changeme

# API auth
JWT_SECRET=change-me-in-production
# Generate: python -c "from passlib.context import CryptContext; print(CryptContext(['bcrypt']).hash('1234'))"
ADMIN_PIN_HASH=

# Integrations (optional for Phase 0)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
CHAT_PROVIDER=openai
CHAT_MODEL=gpt-5
GOCARDLESS_SECRET_ID=
GOCARDLESS_SECRET_KEY=
