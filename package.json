{
  "name": "mr-ducky",
  "private": true,
  "scripts": {
    "install:all": "pnpm install && uv sync --project api",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.33.2"
}
